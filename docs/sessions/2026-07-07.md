# Sessions — 2026-07-07

## 00:42 — Spike executed (browser ✅, device in flight) + design imported
- did: Browser spike built & verified clean (Opus agent): `spike/sample.html` torture test → 4-page A4 PDF, math/code/tables all correct. Minimal device test app scaffolded at `spike/device/` (tsc + expo export verified). Design pulled from claude.ai/design via DesignSync (driver-only tool — subagents can't see it) into `design/`; Sonnet distilled `design/DESIGN-SPEC.md` (5 screens, exact tokens, no PRD gaps). Raja's phone connected via adb (USB debugging enabled); diagnosed Expo Go 54.0.8 = SDK 54 vs scaffold's SDK 57 → "requires newer Expo Go" error. Sonnet agent launched (still running at checkpoint): SDK downgrade + fully automated on-device test via adb reverse + HTTP POST-back of the PDF.
- changed: `spike/` (all new, untracked), `design/` (all new, untracked). docs/ untouched until device verdict.
- decided: device testing = Sonnet via adb, autonomous (Raja's explicit ask); pin SDK 54; PDF exfil via POST to adb-reversed port (Expo Go cache unreadable) → decisions.md
- next: read `spike/DEVICE-FINDINGS.md` when the agent finishes → write go/no-go verdict into decisions.md → close issue #2 → issue #3 scaffold (TDD, SDK 54). Ask Raja: PR-per-issue vs commit-per-issue (unanswered).

## 01:00 — Device spike verdict: GO. Issue #2 closed.
- did: Sonnet agent ran the full on-device test autonomously (SDK 54 downgrade, adb-driven, PDF POSTed back). Driver verified pages visually. Verdict GO; A4→Letter defect logged with fix (explicit width/height in printToFileAsync). Issue #2 closed on GitHub with evidence links.
- changed: spike/DEVICE-FINDINGS.md, spike/device-output.pdf, spike/compare/, spike/device/App.tsx (SDK 54 + POST-back), docs/decisions.md, docs/STATE.md.
- decided: GO on expo-print; Capacitor fallback retired → decisions.md
- next: issue #3 scaffold (SDK 54, jest+jest-expo, TDD). Fresh session starts here.

## 01:51 — Overnight run: Phase 1 research + issue #3 scaffold done
- did: filed #10 (PDF quality gate spec) + #11 (capture-helper prompts) from Sonnet research; issue #3 scaffold implemented (Opus), verified (jest/tsc/expo-doctor), codex-reviewed (3 findings fixed: no dev-client, gitignore, README adb), on-device boot PASS in Expo Go 54.
- changed: full Expo SDK 54 scaffold at repo root, eas.json, README, .gitignore, src/ skeleton; commits e27998d + 62e650f.
- decided: no expo-dev-client (keeps Expo Go workflow) → decisions.md; push-to-main blocked by policy → work rides on `overnight-build` branch.
- next: issue #4 parse layer (include #11's parser-hardening checklist).

## 02:15 — Issue #4 parse layer done
- did: markdownToHtml + normalizeInput (TDD, 42 tests); codex review round (2 majors fixed: whole-doc-only fence unwrap, code-region-protected rewrites; strict:'error' KaTeX fallback).
- changed: src/parse/* + tests.
- decided: nothing new load-bearing.
- next: issue #5 render layer (print CSS per issue #10 spec + Letter-size fix width:595/height:842).

## 02:50 — Issue #5 render layer done (the quality core)
- did: print.css per #10 spec + KaTeX full-font embedding (360KB) + renderToPdf Letter-size fix; codex round (line-count breakability, no display-math clipping); on-device torture test PASS (A4, header repeat, 737ms). Driver visually verified page renders — quality above AI exports.
- changed: src/render/*, scripts/embed-katex-css.js, spike/torture.md.
- decided: display math overflows visibly rather than clips (data loss worse than overflow) — in code comment.
- next: issue #6 capture layer.

## 03:05 — Issue #6 capture layer done
- did: capture module (clean/heuristic/clipboard/suggestion/hook) + App.tsx flow; device test PASS; codex round applied (7 items — clipboard retry+request-id, reentrancy guard, empty-box-only suggestions, dismissed lifecycle, anchored artifact strip, KeyboardAvoidingView).
- changed: src/capture/*, App.tsx, expo-clipboard dep.
- decided: clipboard suggestion only offered when paste box is empty (no overwrite temptation).
- next: issue #7 output layer (save/share).

## 03:30 — Issue #7 output layer done
- did: share/print/SAF-save + suggestFilename; device test PASS (all 3 flows, file verified in /sdcard/Documents); codex round fixed (preflight staleness, failure tests, size guard, filename snapshot).
- changed: src/output/*, App.tsx.
- decided: SAF staleness = readDirectoryAsync preflight failure ONLY; other save errors surface typed.
- next: issue #8 storage layer (AsyncStorage history).

## 03:40 — Issue #8 storage layer done
- did: history store (single-key AsyncStorage, durable PDF copies, mutex-serialized mutations, staged regenerate swap); codex round fixed (3 highs).
- changed: src/storage/*, src/types, App.tsx.
- decided: single-key JSON array over per-doc keys (atomicity without transactions).
- next: issue #9 — UI screens per design/DESIGN-SPEC.md (the big one).

## 08:53 — Morning continuation: #9 review-fix-ship, #10, #11 done
- did: resumed after overnight limit hit. #9 was implemented but unreviewed → codex review (REQUEST-CHANGES, 4 findings) → Opus fixed (in-flight guard, cancel-safe save, HTML-snapshot preview, pageCount §1e) → committed 4b79074, closed. #10: spec already implemented; measure 40em→34em + 13 gate tests → 6bc0b69, closed. #11: helper prompts + sheet built by Opus, codex review (2 findings: copy-failure UX, sheet scroll) → fixed → e337bf4, closed. 248 tests green. On-device smoke test dispatched (Sonnet/adb).
- changed: App.tsx, src/ui/** (new layer + HelperSheet), src/capture/{helperPrompts,clipboard}, src/render/{print.css,toPdf}, src/storage/history, src/types.
- decided: Preview = persisted HTML snapshot (htmlUri) — no PDF viewer in Expo Go; snapshot prevents history drift. → decisions.md
- next: consume smoke-test report → fix any blockers → Phase 3 EAS build (blocked on Raja: npx eas-cli login).

## 14:46 — Beautiful code cards + PDF naming + Documents save
- did: (1) Redesigned code blocks into carded `.codeblock` (header bar w/ dots + language label, JetBrains Mono embedded woff2, highlight.js parse-time highlighting, grayscale-safe theme) — sent a Sonnet research agent first for the approach, then implemented. (2) Save now opens a "Name your PDF" dialog pre-filled with the title + saves to public Documents via a one-time SAF grant pre-seeded to Documents. (3) Committed pending app.json (EAS link). Verified render in Chromium (caught+fixed a header-table padding overflow clipping the language label) and drove the full flow on-device via a Sonnet adb agent — file confirmed at /sdcard/Documents/My Math Notes.pdf, no crashes. Suite 248→282 green.
- changed: NEW scripts/embed-code-font.js, src/render/codeFont/codeFontCss.ts, src/ui/components/NameDialog.tsx; edited src/parse/markdownToHtml.ts, src/render/{print.css,template}.ts, src/output/{saf,filename,index}.ts, src/ui/{index,screens/PreviewScreen}.tsx, App.tsx, package.json; +tests. Commits ba36923, 629cbc1, c4fdca9 (main, NOT pushed).
- decided: highlight at parse time (not print); JetBrains Mono embedded; naming + Documents-default save; hold the push until the GitHub email attribution fix -> decisions.md
- next: Raja fixes GitHub email (move rajasaikia1644@gmail.com from Anuraj-Jit-Saikia → Anuraj-dev), then push. Then Phase 3 EAS build after `npx eas-cli login`.
