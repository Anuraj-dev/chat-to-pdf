// [2] PARSE — public API. markdown → HTML (+ static KaTeX).
// See docs/specs/0001-architecture-foundation.md §4. Issue #4 (+ #11 hardening).

export { markdownToHtml } from './markdownToHtml';
export { normalizeInput } from './normalize';
